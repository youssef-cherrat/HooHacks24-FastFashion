{
    "name": "green-threads",
    "engines": {
        "node": "16.x",
        "npm": "8.x"
    },
    "scripts": {
      "heroku-postbuild": "cd frontend && npm install && npm run build"
    },
    "dependencies": {
      "express": "^4.18.2"
    }
  }